import { Text } from 'react-native';

import { Container } from '~components';

const MainScreen = () => {
  return (
    <Container>
      <Text>Main</Text>
    </Container>
  );
};

export default MainScreen;
