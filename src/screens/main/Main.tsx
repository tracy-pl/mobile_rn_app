import { Container } from '~components';
import Header from '../../components/Header'

const MainScreen = () => {
  return (
    <Container>
      <Header />
    </Container>
  );
};

export default MainScreen;
