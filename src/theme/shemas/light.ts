import { colors } from '../colors';
import { Schema } from './types';

const light: Schema = {
  colors: {
    primary: colors.blue,
    text: colors.black,
    background: colors.white,
  },
};

export default light;
