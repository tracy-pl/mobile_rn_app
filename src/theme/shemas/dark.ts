import { colors } from '../colors';
import { Schema } from './types';

const dark: Schema = {
  colors: {
    primary: colors.blue,
    text: colors.white,
    background: colors.black,
  },
};

export default dark;
